# AGENTS.md — entertainment-ai-lab AI エージェント向けインデックス

> **このファイルは北極星（North Star）です。**
> AI ツール（Claude / Copilot / Cursor）はこのファイルを起点にプロジェクトを把握します。
> **このファイルを最初に読み、ここに書かれたルールと制約に従ってください。**

> [!IMPORTANT]
> **このリポジトリはコードではなく「活動ネタ」を管理するドキュメント運用リポジトリです。**
> 構造ファイル（INDEX.md・テンプレート・設定類）は整合性優先で更新してよいが、
> ネタ本文・投稿ドラフトはユーザーの創作物 — 確認なしの書き換えは禁止（追記・提案は歓迎）。

---

## 📋 プロジェクト概要

**entertainment-ai-lab** は、「毎週エンタメ×AI のプロトタイプを公開する研究室」の
**看板リポジトリ**であり、同時に活動ネタ（定期的な開発・X の投稿・生成 AI サービスを
使った取り組み）の**管理リポジトリ**です。

- **スタック**: Markdown ドキュメント運用 + 静的ランディング `docs/index.html`（ビルドなし）
- **対象**: 対外的なショーケース（README / GitHub Pages）+ 個人の活動管理
- **紹介チャネル**: X (Twitter) / note・ブログ / YouTube・動画

現在の状態：
- 立ち上げ完了（2026-08-11）— ネタ管理の構造・運用ルール・ランディングを整備済み
- Prototype #1: AFTERPOST、#2: vlog-cockpit を README と `docs/index.html` に掲載済み
- 登録済みのネタはまだ 0 件
- **次のタスク: 最初のネタを `/add-neta` で登録する**

---

## 🗂 ネタ管理の仕組み

- 1ネタ = 1ファイル。`neta/<カテゴリ>/YYYY-MM-DD-<slug>.md`（雛形は `neta/TEMPLATE.md`）
- カテゴリ: `dev`（定期開発）/ `x-post`（X投稿）/ `gen-ai`（生成AI実験）
- ステータスフロー: `idea` → `doing` → `published`（見送りは `dropped`）
- `neta/INDEX.md` がステータス別一覧のハブ。各ファイルの frontmatter と常に整合させる
- 週次棚卸し（`/review-neta`）で整合確認と「今週やるネタ」の選定を行う
- 詳細な運用ルールは `CLAUDE.md`、決定の経緯は `ADR.md`（ADR-001）を参照

---

## 🔄 ADR 駆動設計

運用方式や構造の変更など、あとから「なぜこうしたのか」を説明したくなる決定は
`ADR.md` に記録します（`/generate-adr` で追記）。

- ✅ 記録する: 管理方式の変更、カテゴリ・ステータス体系の変更、公開フローの変更
- ❌ 記録不要: 個々のネタの追加・更新、誤字修正

---

## 🤖 エージェント

| エージェント | 用途 | 呼び出しタイミング |
|------------|------|----------------|
| `@Main` | 自律フルパイプライン（explore→plan→implement→verify→review） | 構造・運用の変更 |
| `@Plan` | 設計・複数案提示 | 管理方式を変えたい際 |
| `@Explore` | 読み取り専用調査・Q&A | ネタの検索・集計・質問 |
| `@Implementer` | 承認済み計画の実行（@Plan 経由のみ） | Plan 承認後 |
| `@Reviewer` | 品質・整合性監査 | 構造変更後 |
| `@Verification` | 整合チェックの実行 | 変更後の確認 |

---

## 📌 次のステップ

1. **最初のネタを登録する** — `/add-neta <ネタの説明>` を実行
2. **週次棚卸しを習慣化する** — 週1回 `/review-neta` を実行
3. **公開したら記録する** — `published` にして frontmatter の `links` に公開 URL を残す

---

## 🚫 絶対守るべき制約

- `neta/**` のネタ本文・投稿ドラフトをユーザー確認なしに削除・書き換えしない（追記・提案は可）
- `status: published` のネタ本文は公開記録 — 遡及的な変更禁止
- ネタの追加・ステータス変更時は `neta/INDEX.md` を必ず同時に更新する
- プロトタイプの一覧は README の Prototypes 表と `docs/index.html` の2箇所 — 常に同期させる
- X・note 等への実際の投稿はユーザーが行う。AI は下書き・提案まで

---

## 📚 参考

- エージェント定義は `.github/agents/` ディレクトリに存在
- ネタファイルの規約は `.github/instructions/` ディレクトリに存在
- スキル（手順書）は `skills/` ディレクトリに存在
- アーキテクチャ決定は `ADR.md` に記録

---

**Last Updated**: 2026-08-11
**Version**: 2.0
