# ADR — Architecture Decision Records

アーキテクチャ決定の台帳です。重要な決定とその根拠を記録します。

---

## 記録テンプレート

新しい決定を記録する際は、以下のテンプレートをコピーして追記してください。

```markdown
## ADR-{番号}: {タイトル}

- **日付**: YYYY-MM-DD
- **状態**: 提案中 / 承認済み / 却下 / 廃止
- **決定者**: {名前 or チーム}

### コンテキスト
{なぜこの決定が必要になったか。背景・制約を記述。}

### 検討した選択肢

| 選択肢 | メリット | デメリット |
|--------|---------|-----------|
| A: {選択肢A} | ... | ... |
| B: {選択肢B} | ... | ... |

### 決定
{選択した選択肢と理由}

### 影響
{この決定が与える影響・トレードオフ}
```

---

## 決定履歴

<!-- 新しい ADR をここに追記していく -->

## ADR-001: ネタ管理方式 — リポジトリ内 Markdown ファイル管理

- **日付**: 2026-08-11
- **状態**: 承認済み
- **決定者**: inatani

### コンテキスト

entertainment-ai-lab の活動（定期的な開発・X の投稿・生成 AI サービスを使った取り組み）の
ネタを一元管理し、対外的に紹介できる状態に育てたい。ネタの記録・ステータス管理・
投稿ドラフトの置き場をどこにするかを決める必要があった。

### 検討した選択肢

| 選択肢 | メリット | デメリット |
|--------|---------|-----------|
| A: リポジトリ内 Markdown 管理 | オフライン完結。git で履歴が残る。Claude が検索・集計しやすい。リモート不要で今すぐ始められる | 通知・自動化は Issues に劣る。INDEX とファイルの2箇所更新が必要 |
| B: GitHub Issues 管理 | ラベル・マイルストーン・自動化と連携しやすい | GitHub リモートが前提。オフラインで書けない。下書き長文の管理に不向き |
| C: ハイブリッド（ネタ帳=Markdown、実行=Issues） | 役割分担が明確 | 2段階の運用が手間。個人運用にはオーバースペック |

### 決定

**A: リポジトリ内 Markdown 管理** を採用する。

- 1ネタ = 1ファイル。`neta/<カテゴリ>/YYYY-MM-DD-<slug>.md`
- カテゴリはディレクトリで分ける: `neta/dev/`（定期開発）/ `neta/x-posts/`（X投稿）/ `neta/gen-ai/`（生成AI実験）
- `neta/INDEX.md` がステータス別の一覧（各ファイルの frontmatter と常に整合させる）
- ステータスフロー: `idea` → `doing` → `published`（見送りは `dropped`）
- 運用サイクルは週次棚卸し（`/review-neta`）

将来、開発ネタの実装を並行タスク化したくなったら、該当ネタのみ ticket-breakdown で
GitHub Issue 化する（C への部分的な移行パスを残す）。

### 影響

- このリポジトリがネタの単一の情報源（single source of truth）になる
- ステータス更新はネタファイルと INDEX.md の2箇所 — 週次棚卸しで整合を確認する運用でカバー
- GitHub リモートがなくても運用開始できる

## ADR-002: 公開ランディングは GitHub Pages（main ブランチ /docs 配信）

- **日付**: 2026-08-11
- **状態**: 承認済み
- **決定者**: inatani

### コンテキスト

過去の制作物を「バラバラの個人開発」ではなく「一貫した実験シリーズ」として見せるため、
プロトタイプ一覧の1枚ランディング（`docs/index.html`）を公開する。ホスティング先を決める必要があった。

### 検討した選択肢

| 選択肢 | メリット | デメリット |
|--------|---------|-----------|
| A: GitHub Pages（/docs 配信） | リポジトリを GitHub に置くだけで完結。ビルド不要。追加サービス不要 | 無料プランではリポジトリを public にする必要がある（ネタ帳も公開になる） |
| B: Vercel | vlog-cockpit と同居。カスタムドメイン・プレビューが容易 | プロジェクト設定が別途必要。静的1枚にはオーバースペック |

### 決定

**A: GitHub Pages** を採用する。リポジトリは public（inai17ibar/entertainment-ai-lab）で運用し、
ネタ帳（`neta/`）も公開運用とする（「公開で実験する研究室」というコンセプトと一致）。
公開 URL: https://inai17ibar.github.io/entertainment-ai-lab/

### 影響

- `docs/` 配下がそのまま公開ページ — 公開したくない下書きは `docs/` に置かない
- `neta/` を含むリポジトリ全体が public — 秘密情報（API キー・未公開の個人情報）は書かない
- プロトタイプ追加時は README の表と `docs/index.html` を同期させる（AGENTS.md の制約に記載）
